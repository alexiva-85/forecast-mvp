/** Короткие тексты подсказок (? ) — только сложные термины. */

export const TRADE_HINTS = {
  orderType:
    "Лимит — ваша цена, остаток может ждать в стакане. Рынок — сразу по лучшим заявкам.",
  execution:
    "IOC — исполнить сколько есть, остаток отменить. FOK — только весь объём сразу, иначе отмена.",
  estimate:
    "Оценка по текущей цене. Комиссия списывается при исполнении, пополам у сторон сделки.",
} as const;

export const EXECUTION_INLINE: Record<"ioc" | "fok", string> = {
  ioc: "Сразу по стакану; неисполненный остаток отменяется.",
  fok: "Только полный объём; если в стакане мало — отмена без сделки.",
};
